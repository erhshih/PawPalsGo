{
  "name": "pawpals-go",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "mobile": "pnpm --filter mobile start",
    "web": "pnpm --filter web dev",
    "backend": "pnpm --filter backend start:dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "dependencies": {
    "dotenv-cli": "^11.0.0"
  }
}

# ── 依賴套件 ──────────────────────────────────────
node_modules

# ── 環境變數（絕對不能上傳）────────────────────────
.env
.env.*
!.env.example    # 只允許範例檔案

# ── 系統檔案 ──────────────────────────────────────
.DS_Store
Thumbs.db

# ── 編輯器 / 工具 ─────────────────────────────────
.vscode
.idea
.claude

# ── 建置產出 ──────────────────────────────────────
dist
build
.next
out

# ── 日誌 ──────────────────────────────────────────
*.log
npm-debug.log*
pnpm-debug.log*

# ── 上傳檔案（本機暫存，不需要版本控制）────────────
apps/backend/uploads
